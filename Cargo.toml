[package]
name = "twitchlink"
version = "0.1.0"
authors = ["museun"]
edition = "2018"

[dependencies]
getopts = "0.2.18"
ureq = "0.9.0"
serde_json = "1.0.38"
serde = {version = "1.0.88", features = ["derive"]}

[profile.release]
lto = true
panic = "abort"
