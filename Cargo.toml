[package]
name = "twitchlink"
version = "0.1.0"
authors = ["Travis"]

[dependencies]
#reqwest = "0.8"
curl = "0.4"
serde = "1.0"
#serde_derive = "1.0"
serde_json = "1.0"
failure = "0.1" 
# percent-encoding = "1.0.1"

[profile.release]
lto = true
panic = "abort"